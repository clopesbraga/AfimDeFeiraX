<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".Application.AfimdeFeiraxApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo5"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AfimdeFeiraX"
        tools:targetApi="31">
        <activity
            android:name=".View.ProdutoListActivity"
            android:exported="false" />
        <activity
            android:name=".View.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AfimdeFeiraXTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".View.LoginActivity"
            android:exported="true"></activity>
        <activity
            android:name=".View.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AfimdeFeiraX.NoActionBar"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_maps_key" />
    </application>

</manifest>